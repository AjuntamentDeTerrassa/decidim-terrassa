#!/usr/bin/env bash
set -e

# Install yarn globally via npm if it's not already available
if ! command -v yarn &> /dev/null; then
  echo "-----> Installing Yarn via npm..."
  npm install -g yarn
  echo "✓ Yarn installed successfully"
else
  echo "✓ Yarn is already installed"
fi

